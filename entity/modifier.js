class Modifier {
    /**
     * 
     * @param {string} stat 
     * @param {number} value 
     */
    constructor(stat, value) {
        this.stat = stat;
        this.value = value;
    }
}

module.exports = Modifier;